
import type { Metadata } from "next";
import SapalonPage from "../page";

export const metadata: Metadata = {
  title: "Sapalon Services",
  description: "Expanded engineering and technical services from Sapalon Ghana Limited.",
};

export default function ServicesPage() {
  return <SapalonPage />;
}
